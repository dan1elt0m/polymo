[project]
name = "jsonplaceholder_demo"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31",
]

[build-system]
requires = ["uv_build>=0.7.19,<1.0"]
build-backend = "uv_build"
